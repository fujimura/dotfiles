[core]
  pager        = less
  editor       = vim
  excludesfile = ~/.gitignore
  [color]
  status          = auto
  diff            = auto
[alias]
  st              = status -bs
  s               = status -bs
  di              = diff
  d               = diff
  ci              = commit -v
  fixup           = commit --amend -C HEAD
  reset1          = reset HEAD~1
  co              = checkout
  ad              = add
  l               = log --decorate
  lp              = log --decorate --stat -p --graph
  lr              = log --stat -p --reverse
  r               = rebase
  br              = branch
  b               = branch
  f               = fetch
  g               = grep -n -p --color --heading --break
  fu              = fetch upstream
  mum             = merge upstream/master
  rum             = rebase upstream/master
  current-branch  = "!sh -c \"git branch | grep '*' | awk '{ print \\$2 }' \""
  push-for-review = "!sh -c 'git push origin HEAD:refs/for/master/`git current-branch`'"
[user]
  name = Fujimura Daisuke
  email = dfujimura@aiming-inc.com
[github]
  user            = fujimura
